# Required for Railway (or local) deployment. Copy to .env and set values.
# DATABASE_URL - PostgreSQL connection string (Railway provides this)
DATABASE_URL=postgresql://user:pass@host:port/dbname
# JWT_SECRET - Secret for signing JWTs (set a strong value in production)
JWT_SECRET=your-jwt-secret-change-in-production
# FRONTEND_URL - Allowed CORS origin, e.g. https://your-app.vercel.app
FRONTEND_URL=https://your-frontend.vercel.app
# NODE_ENV - Set to "production" on Railway
NODE_ENV=production
# SEED_SECRET - Optional. If set, POST /seed with header X-Seed-Secret: <value> runs the dummy user seed (for prod when Shell is not available).
# SEED_SECRET=your-one-time-secret
